org.gradle.jvmargs=-Xmx1G
# Mod version (semver; '+loader' suffix added at packaging)
mod_version=1.0.0
# Group / package base
group=org.refcraft
archives_base_name=AsyncScreenshot
# Java release target
java_version=17
